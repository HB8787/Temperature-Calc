# Temperature-Calc (1p13)

This code is extracting daily temperature data (given in code not from the internet), sorting the data, and 
then calculating the median temperature for a given set of data.
